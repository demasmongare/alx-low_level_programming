#include "main.h"
/**
 * mul - function that multiplies two integers
 * @a: integer to be multiplied
 * @b: interer to be multiplied 
 *
 * Return: a * b
 */
int mul(int a, int b)
{
    return (a * b);
}
