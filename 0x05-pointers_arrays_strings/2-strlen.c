#include "main.h"
/**
 * _strlen - returns the length of a string
 * @s: function parameter
 *
 * Return: length of a string
 */
int _strlen(char *s)
{
	int i = 0;

	if (s[i] != '\0')
		while (i < s[i])
		{
			i++;
		}
	return (i);

}
